{
  "name": "url-shortener-backend-development",
  "version": "1.0.0",
  "description": "The goal is to create a RESTfull API for a URL shortener service, allowing users to transform long URLs into concise version",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "Mahmood Ashraf",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
